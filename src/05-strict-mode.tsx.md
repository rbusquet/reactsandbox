```ts
import "stop-runaway-react-effects/hijack";

// import React from "react";

function App() {
  // const [state, setState] = React.useState({ status: "idle", count: 0 });

  // React.useEffect(() => {
  //   setState({ ...state, count: state.count + 1 });
  // }, [state]);
  return null;
}

export { App };
```
